import { Stage, Layer } from "react-konva";

export default function EditorCanvas() {
  return (
    <Stage width={794} height={1123}>
      <Layer>
        {/* Elemente (Text, QR, Bilder) kommen später hier rein */}
      </Layer>
    </Stage>
  );
}
